function somaNumerosArray (numeros) {
    const retorno = []
    const primeiroNumero = [0] /2 
    const ultimoNumro = numeros[numeros.length-1] /2 //-1 porque sempre começa do 0 
    retorno.push(ultimoNumro)
    //retorno[0] = ultimoNumero
    //retorno[1] = primeiroNumero
    return retorno; 
}

